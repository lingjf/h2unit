#include "../source/h2_unit.h"

H2UNIT (h2_cfg) {
   void setup()
   {
   }

   void teardown()
   {
   }
};

// H2CASE(h2_cfg, "match empty")
// {
//     h2_cfg cfg;

//     H2EQ(-1, cfg.match("welcome"));
// }

// H2CASE(h2_cfg, "match fail")
// {
//     h2_cfg cfg;

//     H2EQ(-1==cfg.match("welcome"));
// }
